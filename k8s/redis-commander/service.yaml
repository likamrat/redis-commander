---
apiVersion: v1
kind: Service
namespace: redisdb
metadata:
  name: redis-commander
  labels:
    app: redis-commander
    tier: backend
spec:
  ports:
    - port: 8081
      targetPort: redis-commander
      name: redis-commander
  type: LoadBalancer
  selector:
    app: redis-commander
    tier: backend
